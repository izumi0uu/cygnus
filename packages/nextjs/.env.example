# Template for NextJS environment variables.
# This file is used ONLY by the frontend (packages/nextjs).
# Copy it to `.env.local` and fill in the real values.

# ------------------------------------------------------------------------------
# Frontend App
# ------------------------------------------------------------------------------

# Public base URL of the backend API (NestJS), used by the browser.
NEXT_PUBLIC_API_URL=http://localhost:3000

# ------------------------------------------------------------------------------
# Privy (Frontend only)
# ------------------------------------------------------------------------------

# Privy App ID for frontend SDK (from Privy Dashboard).
# This value is public and safe to expose in the browser.
NEXT_PUBLIC_PRIVY_APP_ID=

# ------------------------------------------------------------------------------
# Ethereum RPC / Alchemy
# ------------------------------------------------------------------------------

# Alchemy API key used by scaffold-eth config (optional).
# If empty, will fall back to the default scaffold-eth key.
NEXT_PUBLIC_ALCHEMY_API_KEY=

# ------------------------------------------------------------------------------
# WalletConnect / RainbowKit
# ------------------------------------------------------------------------------

# WalletConnect project ID (for RainbowKit).
NEXT_PUBLIC_WALLET_CONNECT_PROJECT_ID=

# ------------------------------------------------------------------------------
# IPFS build flag (used by scaffold-eth for IPFS deploys)
# ------------------------------------------------------------------------------

# When set to "true", enables IPFS build mode.
NEXT_PUBLIC_IPFS_BUILD=false